# Kairo

Kairo 是一个用 Python 构建的 HTTP(S) 调试代理工具（V1 开发中）。

## 当前已完成
- FastAPI API 骨架
- SQLAlchemy 核心模型（Project / HttpFlow / Intercept）
- `GET /health` 与 `GET /api/v1/flows` 接口

## 本地启动
```bash
uv run main.py
```
